import React from 'react';
import { selectSong } from '../actions' ;
    // we type only actions and not action/index.js because thanks to webpack, if we name
    // our file index.js, it will be used and we don't have to specify it in our actions folder

const App = () => {
    return <div>App</div>
};

export default App;
